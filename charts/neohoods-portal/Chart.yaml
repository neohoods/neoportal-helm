apiVersion: v2
name: neohoods-portal-chart
description: A Helm chart for deploying NeoHoods portal app
type: application
version: 0.2.67
appVersion: "2.0.0"
# PostgreSQL is now deployed via custom template (postgresql-statefulset.yaml) using official postgres image
# dependencies:
#   - name: postgresql
#     version: "12.x.x"
#     repository: "https://charts.bitnami.com/bitnami"
